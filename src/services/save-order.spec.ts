describe('testing some service', () => {
  it('should return 1', () => {
    const number = 1;
    expect(number).toBe(1);
  });
});

describe('testing some service', () => {
  test('should return Rafael', () => {
    const nome = 'Rafael';
    expect(nome).toBe('Rafael');
  });
});
